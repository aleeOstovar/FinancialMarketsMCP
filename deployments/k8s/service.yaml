apiVersion: v1
kind: Service
metadata:
  name: mcp-cmc-service
  namespace: mcp-services
spec:
  selector:
    app: mcp-cmc
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP